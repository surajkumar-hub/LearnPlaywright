// Functions

// Define of a function or defination of a function - Step 1
function greet() {
    console.log("Hi, how are you?") // If we run this function nothing will 
    // happen because it's just a defination. To make this print in cosole, 
    // we have to call it. That is step -2
}

// calling of the function - Step 2
greet();