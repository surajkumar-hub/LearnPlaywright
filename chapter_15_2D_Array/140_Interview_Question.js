let matrix_2D = [
    [1, 2, 3, 4],
];

// What is the size of the matrix
// R  - 1
// C - 4
// Size is = 1x4

// When a matrix has the same number of rows and columns, it is called a grid

let grid = [
    [10, 20, 30],
    [40, 50, 60],
    [70, 80, 90]
];

// How to access it - By using the indexation

console.log(grid[0][0]) // 10
console.log(grid[0][2]) // 30

console.log(grid.length);   // 3 — number of rows
console.log(grid[0].length);  // 3 — number of cols in row 0