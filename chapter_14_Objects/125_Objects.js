let obj = {}; // This is an empty object and it doesn't have key - value pair

let obj1 = {
    name: " Pramod",
    age: 42,
    rollNo: 123
};

// Object in JS looks like json but not actually json.
// JSON looks like this - { "name" : "Pramod"}

let array = [1, 2, 3, 4]; // Array looks like this

let test_Obj_ref = { name: "Pramod", age: 65, phone: 9876543210 };
// We can define object like this also in JS